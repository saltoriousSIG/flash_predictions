{
  "name": "flash-predictions",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "contracts",
    "web_app"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@flash-predictions/web_app",
    "build:web": "npm run build --workspace=@flash-predictions/web_app",
    "vercel-build": "npm run build --workspace=@flash-predictions/web_app",
    "dev:contracts": "npm run node --workspace=@flash-predictions/contracts",
    "compile:contracts": "npm run compile --workspace=@flash-predictions/contracts",
    "test:contracts": "npm run test --workspace=@flash-predictions/contracts",
    "test:snap": "node scripts/test-snap.mjs"
  },
  "engines": {
    "node": ">=22.11.0"
  }
}
